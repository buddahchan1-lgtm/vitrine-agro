<% if (notice) { %>
  <div class="mb-4 rounded-2xl border px-4 py-3 text-sm shadow-sm <%= notice.type === 'success' ? 'border-emerald-200 bg-emerald-50 text-emerald-800' : 'border-rose-200 bg-rose-50 text-rose-800' %>">
    <%= notice.text %>
  </div>
<% } %>
